[build-system]
requires = ["setuptools>=78.1.1", "setuptools-scm[toml]>=6.2"]
build-backend = "setuptools.build_meta"

[project]
name = "gwyddionpy"
dynamic = ["version"]
authors = [
    { name = "The NOMAD Authors" },
]
description = "Read any Gwyddion-supported SPM raw file into NumPy via the gwyconvert helper"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
]
dependencies = [
    "numpy>=1.22",
    "gwyfile>=0.3",
    "platformdirs>=3.0",
]

[project.urls]
"Homepage" = "https://github.com/FAIRmat-NFDI/gwyddionPy"
"Parent Project" = "https://github.com/FAIRmat-NFDI"

[project.scripts]
gwyddionpy-fetch-converter = "gwyddionpy._fetch_converter:_main"

[project.optional-dependencies]
# The GPL converter is an extra, never a hard dependency: a plain install
# stays Apache-2.0, and the converter has wheels for four platforms and no
# source distribution, so requiring it would break pip anywhere else.
#
# publish.yml rewrites the line below to pin the converter version.
converter = ["gwyddionpy-converter"]
hdf5 = ["h5py>=3.0"]
test = ["pytest>=7", "h5py>=3.0", "hypothesis>=6.0"]
dev = [
    "mypy",
    "ruff>=0.14.0",
    "pytest",
    "pre-commit",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv]
# Both packages are on TestPyPI so far; their dependencies are on real
# PyPI. Drop both lines at production PyPI (also in the converter package).
index-url = "https://test.pypi.org/simple"
extra-index-url = ["https://pypi.org/simple"]

[tool.setuptools_scm]
root = ".."
version_scheme = "no-guess-dev"
local_scheme = "node-and-date"

[tool.ruff]
include = ["src/gwyddionpy/*.py", "src/gwyddionpy/**/*.py", "tests/*.py", "tests/**/*.py"]
lint.select = [
    "E", # pycodestyle
    "W", # pycodestyle
    "PL", # pylint
    "NPY201", # upgrade to numpy>2
]
lint.ignore = [
    "E501", # Line too long ({width} > {limit} characters)
    "E701", # Multiple statements on one line (colon)
    "E731", # Do not assign a lambda expression, use a def
    "E402",  # Module level import not at top of file
    "PLR0911", # Too many return statements
    "PLR0912", # Too many branches
    "PLR0913", # Too many arguments in function definition
    "PLR0915", # Too many statements
    "PLR2004", # Magic value used instead of constant
    "PLW0603", # Using the global statement
    "PLW2901", # redefined-loop-name
    "PLR1714", # consider-using-in
    "PLR5501", # else-if-used
    "PLC0415", # import outside-toplevel
]
lint.fixable = ["ALL"]

[tool.mypy]
strict = false
ignore_missing_imports = true
follow_imports = "silent"
no_strict_optional = true
disable_error_code = "import, annotation-unchecked"

[tool.pytest.ini_options]
testpaths = ["tests"]
# Makes tests/helpers/ importable everywhere, with no sys.path juggling.
pythonpath = ["tests"]
markers = [
    "corpus: tests needing full-size raw files fetched from the corpus host",
    "formats: tests reading the real vendor files under tests/data/",
    "platform: cross-platform tests; run only when --require-platform (or GWYDDIONPY_REQUIRE_PLATFORM) names the platform",
]
