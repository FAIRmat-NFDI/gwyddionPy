"""Test framework helpers, importable from any test group.

Made importable via ``pythonpath = ["tests"]`` in gwyddionpy/pyproject.toml
(pytest's own ini option, no sys.path juggling in conftest).
"""
